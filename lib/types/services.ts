export type Service = {
    icon: string
    id: number
    name: string
    slug: string
    price: number
    duration_minutes: number
    description?: string
    is_active: boolean
    color?: string
}